[package]
name = "crypt_guard_examples"
version = "2.0.5"
edition = "2021"
description = "Examples for the crypt_guard safe API and legacy compatibility paths."
license = "MIT"
repository = "https://github.com/mm9942/crypt_guard"

[dependencies]
crypt_guard = { path = "..", version = "2.0.5" }
